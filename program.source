VAR x, squ, v[5];

PROCEDURE square(a);
BEGIN
  RETURN a * a;
END;

PROCEDURE exec;
BEGIN
  FOR (x := 1 + 2; x < 10; x := x + 1)
  BEGIN
    CALL square(x);
    !squ;
  END;
END;

BEGIN
  CALL exec;
  v[1] := 5;
  v[0] := v[1] % 2;
  v[1] := square(5);
END.